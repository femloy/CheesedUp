// Make sure this works with the mod disabled (so don't reference modded stuff, unless you know what you're doing)
return
{
    enable: true, // Enable any time
    disable: room == Mainmenu // Only disable in main menu
};
